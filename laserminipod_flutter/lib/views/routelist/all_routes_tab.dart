import 'package:flutter/material.dart';

class AllRoutesTab extends StatelessWidget {
  const AllRoutesTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
